CLI only.

STATUS = INCOMPLETE @ move() and copy() , UNTESTED @ all

DOUBT:
    Should name or path be updated in:
        fofiName = xInput("Enter new name (without extensions): ", BOLD + RED, BOLD + GREEN)
